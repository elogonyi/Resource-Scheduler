name: CI/CD Pipeline

on:
  push:
    branches:
      - main
      - dev  # Adjust as needed
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Debug File Structure
        run: ls -R  # List files to verify structure

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install Dependencies
        run: npm install
        working-directory: ./Resource_Scheduler  # Adjust this if your project is in a subfolder

      - name: Run Tests
        run: npm test
        working-directory: ./Resource_Scheduler  # Adjust if necessary

      - name: Build Application
        run: npm run build
        working-directory: ./Resource_Scheduler  # Adjust if necessary

  deploy:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Deploy Application
        run: |
          echo "Deploying the app..."
          # Add deployment steps (e.g., SSH into server, upload files, restart services)
